import os
import webbrowser

# Link to open (received from the sender script)
link_to_open = 'https://example.com'  # Set the default link or leave it empty

# Open the link in the default web browser
webbrowser.open(link_to_open)
